# Conformance strategy

*Stub — see `DOCUMENTATION_PLAN.md`: where each class of spec rule is
enforced (writer-time vs `ConformanceChecker` vs `check_coverage`), policy
for anything beyond the standard.*
